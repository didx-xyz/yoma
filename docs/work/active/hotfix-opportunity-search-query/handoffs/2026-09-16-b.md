# Handoff: broader search-query hotfix — 2026-09-16

## Scope

Adrian requested the fix wherever the same pattern applies, with no remote changes. Main checkout is now on hotfix/opportunity-search-query for VS review (the earlier handoff described its state before that switch).

See ../feature.md for the cross-domain audit and supplied Production SQL comparison. Previous trigram indexes remain essential and are not removed.

## Changes

Three repositories received cross-table matching changes; four additional services received bounded split hydration. MyOpportunity also batches pending participant counts using the already-tested engagement query.

At Adrian's request, all active PaginationFilter-based paging sites now share PaginationExtensions.Page (IQueryable or IEnumerable). All five split-hydration searches share ToPageWithChildren, including count and the unpaginated path. All ten repository query-Contains overloads delegate through shared infrastructure RepositorySearchExtensions.WhereContains to their predicate overload. Deliberate batching/provider offset loops are unchanged. This increases the file count primarily through one-line reuse changes.

Search fields, null/empty checks, wildcard semantics, caller filtering, ordering, count-only and unpaginated behaviour are retained. Matching sets remain IQueryable, not broad materialized ID lists.

## Validation

- Full solution build passed, zero warnings/errors.
- Fifteen focused API tests passed (nine paging tests, two repository-search composition tests and four organization-status event cases). Eight temporary UI pagination checks passed before their test file was removed at Adrian's request. The retained hook passes ESLint, focused TypeScript and Prettier checks; no UI test file is included. Touched-file formatting and git diff --check passed.
- External local harness: C:/Users/adria/AppData/Local/Temp/yoma-search-wide-validation.
- 192 PostgreSQL comparisons passed, including both Contains overloads, predicate OR composition, preserved caller filters, no matches, case, null/empty values, wildcard terms, overlapping sources, null opportunity links, shared paging-helper counts, complete projected pages and populated child collections. Synthetic inserts are rolled back.
- The opportunity/engagement suite was rebuilt against the final candidate and all 2,920 assertions passed, including a real database reassignment between page-ID selection and hydration; the moved row is excluded and the other rows remain in order.
- Existing referral tests: 40 total, 17 failures on both the unchanged production baseline and candidate, with identical failing test identities. These tests are not green; no unrelated test fixes included.
- dotnet test is blocked by the existing .NET 10/Microsoft.Testing.Platform runner setup; the compiled xUnit executable was used directly, without changing project configuration.
- Harnesses are outside the repository and use an isolated PostgreSQL container; they are not full HTTP/provider/authorization integration tests.

## Remaining

Final extra review included committed candidate changes against production baseline, the helper opt-out and UI fix. It identified and corrected an authorization race: unrestricted hydration could return a freshly reassigned row outside the caller's organization. All five searches now capture their fully non-text-filtered query for hydration, retaining ownership, visibility and other non-text filters without repeating expensive text matching. Text blocks were moved intact to the final filtering stage where needed. Real PostgreSQL before/after reassignment coverage passes. This is readiness for master/Stage validation, not completed end-to-end Production validation. No remote operations performed.

### Follow-up review: UI pagination and regex warnings

- Read-only web inspection: PaginationButtons calculates total pages from totalItems/pageSize; opportunity search and the inspected admin/MyOpportunity screens supply totalCount. A short nonempty page does not disable Next prematurely.
- Jason handoff (implemented with Adrian's approval): usePaginatedQuery now advances using lastPageParam and pageSize against totalCount, with an empty-page stop. Previously accumulated item count could leave See More Programs available after the final real page. Short nonempty pages still continue. ReferrerProgramsList is the sole current consumer. Temporary regression cases passed, but Adrian explicitly requested removal of the UI test file; it is not included. No dependency added. Smoke-test See More through the final page on Stage; concurrent complete deletion of an intermediate page still requires refresh.
- Replaced the three constant regex calls in JobJackClient with GeneratedRegex partial methods, preserving patterns, options and replacements. Full solution build passed with zero warnings/errors; git diff --check passed.
- Final IDE cleanup: organization-status test constant arrays use static readonly fields; build and 15 focused tests pass. Reported baseUrl-removal diagnostic does not match this checkout's pinned/installed TypeScript 5.4.5 and likely comes from a newer IDE TypeScript service. Web tsconfig was left unchanged; align editor/compiler versions rather than bundling a TypeScript configuration migration into the hotfix.
- Explicit lock-aware repository retrievals remain separate from the paging helper. OrganizationStatusChangedEventHandler now explicitly passes hydratePageSeparately: false through a Search overload to ToPageWithChildren. The helper retains the original count plus filtered page/split-query path for this caller; no ID-selection/hydration gap is added and no transaction is held across event publication. This preserves baseline concurrency semantics, not a new snapshot guarantee. Internal marketplace access-rule searches inspected are unpaginated and bypass ID-first hydration.
- Follow-up validation: solution build zero warnings/errors; 15 focused API tests pass (including guarded hydration, original-query fallback, all three event-producing organization statuses, short pages and declined no-op); focused hook ESLint, TypeScript and Prettier checks pass. Full web TypeScript check fails on missing image-module declarations elsewhere in this checkout; full web build/Stage smoke test remains required.

- Review the expanded diff and run full API Stage tests before release, including populated/empty pages, permissions and CF combinations on master.
- The two-step page load adds a database statement while removing repeated search work from split queries. No blanket speedup is claimed for every endpoint.
- Separate count/page/hydration statements retain the earlier documented concurrent-change limitation; no snapshot transaction added.
- Partner-sync per-item calls and computed distribution-list matching remain unchanged.
- CF was inspected on master only. Do not bring master/CF/cash-out into the production current branch.
- TODO after the hotfix merges: audit master-only additions for these patterns and apply helpers where appropriate. Repeat for the pending Umuzi PR; neither is covered by this production-baseline test pass.
- Follow the existing master PR, Stage verification, cherry-pick to current release process. No remote operations, configuration changes or new migration.

### Resume / release checklist

- Adrian's requested extra review is complete, including independent predicate/paging review; hydration access-boundary finding is fixed and regression-tested.
- Local branch remains hotfix/opportunity-search-query: initial candidate is committed, expanded changes and this handoff are not yet committed. Review the complete diff against the production baseline, including untracked helpers/tests; do not inspect only HEAD-to-worktree changes.
- Prepare the master-based hotfix-pr branch, PR/merge to master and deploy to Stage with Adrian. Do not merge master wholesale into current.
- Stage smoke test: country-filtered impact/customer hold opportunity searches, page totals/order, referral See More through the last page, and organization-status event publication. CI must validate the web build; the local whole-web TypeScript check has the documented image-declaration blocker.
- After Stage approval, cherry-pick only the hotfix commits to current and release using Robbie's process with the next agreed release version. No push, merge, tag or deployment has been performed in this work.
