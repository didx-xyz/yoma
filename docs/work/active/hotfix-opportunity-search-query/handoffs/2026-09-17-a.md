# Handoff: Stage smoke checks and local current preparation — 2026-09-17

## Meta

- Feature: opportunity search query hotfix (unticketed)
- Session by: Adrian
- Agent used: Codex
- Branch: `current` (local only; do not push overnight)
- Previous handoff: `2026-09-16-c.md`

## What Was Done

- PR #1939 merged to master as `e3f582f47d2064ff908bfec631f6753e146f5a6f`. This is the canonical squash-merge provenance for the earlier PR branch commits.
- Verified replacement workflow run 35156254639 completed successfully for that SHA: lint, API/web builds on both architectures, image pushes, e2e and Stage deployment. Run 35156245511 was cancelled before deployment.
- Browser automation failed before accessing tabs, so Adrian performed the UI smoke tests. The agent reviewed supplied results; it did not operate or independently inspect Stage through the browser.
- Before cherry-picking, fetched origin/master and origin/current. Local and remote current both matched `v3.12.8` at `f791d5eaf98b1fd3b8d22a380c28fdfca74671bf`.
- Applied the merged PR with `git cherry-pick -x` onto local current. A safety guard initially blocked conflict resolution; the first incomplete attempt was aborted cleanly. Adrian then explicitly asked to finish. A read-only whole-file comparison proved the proposed resolutions equal the original tested production candidate, including one intact, relocated MyOpportunity ValueContains block; resolution was subsequently permitted.
- Resolved only baseline differences: exclude master-only CF filter references (no such feature exists on production), retain the required SSI paging extension import, and retain the reviewed GeneratedRegex cleanup with identical patterns/options. The complete API and web source matches `5c20b2390699fed158e7c7e0c8e57ad714a4e0f0` on the original production-baseline hotfix branch exactly. Differences from that candidate are documentation only.
- No migration, package, Helm, CI or provider configuration changes. No master wholesale merge, remote push, tag or Production deployment.

## Stage Evidence

- Adrian reported passes for opportunity search, filters, paging/detail, profile opportunity tabs, referrals, organisation verifications, action links and store access rules. Final feedback: works and is fast. This is a human browsing smoke pass, not exhaustive regression or a measured Production speedup.
- `impact`, page 1/12, publishedStates [1,0], countries null: 12 items, 12 unique IDs, totalCount 12. Related collections populated; participant totals equal completed plus pending; no hidden returned items. Browser timing: 651.90 ms waiting, 191.27 ms queueing, 846.34 ms total.
- `customer hold` with the same state/country settings: zero items, totalCount zero; initial request had 2.30 seconds waiting. No numeric repeat timing supplied. Neither request reproduces the earlier Production [1,0,2] plus two-country request exactly.
- Active referrals use a carousel. The retired See More modal using usePaginatedQuery is commented out, so the hook change was not exercised in Stage UI. Local validation of that hook remains applicable.
- Organisation-status mutations/event delivery and non-admin authorization were not manually exercised; focused automated tests and earlier synthetic-data checks cover the documented scope only.

## Current State

- Build: full production-baseline solution build, including normal restore, succeeds with zero warnings/errors.
- Tests: 15 focused Core API tests pass on current. Focused frontend hook ESLint and TypeScript checks pass. Earlier PostgreSQL evidence remains 2,920 assertions plus 192 comparisons against the source-identical candidate; not rerun this session.
- Migrations: none.
- Uncommitted work: none after completing this cherry-pick with the handover.
- Release status is in ../feature.md; Production is not marked shipped.

## Next Steps

1. In the morning, fetch origin/current and verify no new remote work appeared overnight. Check current's diff from v3.12.8 is only this hotfix and documentation, and that the checkout is clean. If remote current advanced, reconcile deliberately; never force-push over it.
2. With Adrian, push explicitly `git push origin current:refs/heads/current`, then create the next agreed release targeting current. Do not move/reuse v3.12.8. No unattended release is scheduled.
3. Validate the Production deployment, representative real searches and timings, paging and key admin paths. The prior Stage smoke test is not a guarantee of Production latency.
4. Return to master and audit/apply the helpers and matching optimizations to the remaining master-only functionality, including CF/payout/treasury paths where applicable. Preserve existing filters, access boundaries, ordering and response behavior.
5. Apply the equivalent audit to the pending Umuzi PR after incorporating the shared hotfix. Keep these follow-ups separate from the production-baseline release.

## Gotchas

- Local current is intentionally ahead of origin/current after preparation; do not mistake that for an already released hotfix.
- Canonical PR source is the master squash SHA above. Original branch SHAs in earlier handoffs are pre-squash records retained for comparison, not the release source.
- Full existing referral test suite has previously documented baseline failures. The focused pass does not claim the whole suite is green.

## Open Questions

- Agree the next release version in the morning; no tag created here.
