# Handoff: branch recovery and master PR preparation — 2026-09-16

## Meta

- Feature: opportunity search query hotfix (unticketed)
- Session by: Adrian
- Agent used: Codex
- Branch: `hotfix-pr/opportunity-search-query`
- Previous handoff: `2026-09-16-b.md`

## What Was Done

- The original local hotfix branch was incorrectly tracking `origin/current`. Adrian's VS push advanced `current` to the newly committed, not-yet-Stage-tested hotfix. The wrong upstream was an agent setup mistake.
- Adrian authorized recovery. Verified that remote `current` still pointed to exactly `5c20b2390699fed158e7c7e0c8e57ad714a4e0f0`, then restored it to `f791d5eaf98b1fd3b8d22a380c28fdfca74671bf` using that exact expected-SHA force-with-lease. Verified remote `current` and unchanged tag `v3.12.8` match afterward. No release created or deployment invoked; the configured CICD push trigger is master, not current.
- Removed the incorrect upstream from `hotfix/opportunity-search-query`. Both original commits remain preserved there: `368ebaf55ea8c3e8e3fe3bf1472230a0ec080e77` and `5c20b2390699fed158e7c7e0c8e57ad714a4e0f0`.
- Fetched only origin master and created `hotfix-pr/opportunity-search-query` using `--no-track` from `113a1a13f514bdd90a94f388e53bcaad6d67f55d`. Applied only the two hotfix commits, not the production branch history.
- Resolved overlaps preserving master's custom-field constraints, SSI imports and JobJack request-create contract/existing generated regex helpers. Custom-field constraints remain on opportunity and MyOpportunity page selection and hydration. No duplicate regex helpers retained.
- No Helm, CI, migration or configuration changes. No PR branch push, master merge, release or deployment in this recovery session.

Commit references above were checked locally. They are pre-squash source references; retain the original local hotfix branch and replace/annotate PR commit provenance when the master PR is squash-merged.

## Current State

- Build: complete master-based API solution passes, zero warnings/errors.
- Tests: 19 focused Core API tests pass on this checkout, including paging, repository composition and organization-status event cases.
- Web: the modified pagination hook passes focused ESLint and TypeScript checks. Full web CI/Stage testing remains required; earlier whole-web limitations are in the previous handoff.
- Migrations: none added by this hotfix.
- Uncommitted work: none after completing this cherry-pick with the handover.
- Earlier PostgreSQL harness evidence applies to the production-baseline candidate; those external harnesses were not rerun against the master integration during this recovery.

## Next Steps

1. Push only `hotfix-pr/opportunity-search-query` to the same remote branch, explicitly: `git push --set-upstream origin hotfix-pr/opportunity-search-query:refs/heads/hotfix-pr/opportunity-search-query`. Open the PR with base master. Do this with Adrian; this session leaves the PR branch local.
2. Merge the reviewed PR into master, deploy to Stage, and test the supplied searches, counts/order, referral See More and organization-status event path. Jason's small hook change is documented in the previous handoff.
3. Only after Stage approval, apply the approved hotfix to `current` using Robbie's cherry-pick process. Preserve production-only contracts when resolving any master differences. Do not merge master wholesale into current; do not blindly apply the master-only CF conflict resolutions to production.
4. Push current and cut the next agreed release targeting current. Do not reuse or move `v3.12.8`.
5. Continue the separately recorded master-only/CF and pending Umuzi search-pattern audit after the hotfix merges.

## Gotchas

- Before any push, verify branch name, upstream and the explicit destination. The local PR branch deliberately has no upstream until its first same-name push.
- The original production-baseline hotfix is preserved separately for a scoped comparison during the later current cherry-pick. Both local current and remote current remain at the production tag until Stage approval.
- A branch push is not evidence of a production deployment; no live production health check was performed in this recovery.

## Open Questions

- Next release version is not selected here; agree it with Robbie after Stage testing.
