# Handoff: pre-release public Production benchmark — 2026-09-17

## Meta

- Feature: opportunity search query hotfix (unticketed)
- Session by: Adrian
- Agent used: Codex
- Branch: current, local only
- Previous handoff: 2026-09-17-a.md

## What Was Done

- Adrian requested public Production benchmarks before and after the pending release.
- Added a repeatable PowerShell benchmark under ../benchmarks/, fixed to the public opportunity search endpoint. Five payloads cover the original impact/customer hold country-filtered searches, impact page two, and global-country versions matching the Stage request shape.
- Completed 20 sequential requests, one initial plus three measured per case, with one-second pauses and no retries/concurrency. All returned HTTP 200, no duplicate IDs, stable counts and ordered IDs within cases. No page-one/page-two overlap for country-filtered impact.
- Saved baseline JSON, exact payloads, timestamps, ordered IDs/titles, response hashes and timing summaries. Baseline full HTTP medians: impact country page one 11.623 s, page two 11.639 s, impact global 11.380 s; customer hold country 2.730 s and global 2.718 s. Counts 413/483 for impact, zero for customer hold.
- Verified remote current and v3.12.8 still match the pre-hotfix baseline. No application source changed; API/web source still equals the reviewed production candidate. No push, tag, release, settings changes or data mutations performed.

## Current State

- Benchmark: complete; see ../benchmarks/README.md and before-20260917.json.
- Build/tests: no application changes this session; previous handoff's results apply. Benchmark script parsed and ran successfully.
- Migrations: none.
- Uncommitted work: none after the documentation/benchmark evidence commit.
- Post-release benchmark: pending. No background monitor or release automation scheduled.

## Next Steps

1. Obtain Jason's confirmation, recheck origin/current for intervening work, then follow the separately authorized push/release steps with Adrian.
2. Once deployment is confirmed, rerun the SAME script with Phase after and a fresh filename. Compare exact payload hashes, counts, ordered IDs and median timings for matched cases. Live data changes require investigation, not blind failure/pass.
3. Report measured per-case speedup only after the paired result exists. Do not substitute SQL timings or Stage timings for the Production HTTP baseline.
4. After release verification, return to master for remaining new service/repository optimization coverage, then Umuzi, as requested. Keep those follow-ups out of the production-baseline release.

## Gotchas

- Initial samples are retained but excluded from the three-sample medians consistently before and after.
- Timing includes network/response transfer, not browser rendering; it is not pure server/SQL time or a controlled load benchmark.
- Public search only, no admin or authenticated workload claims.
- This local commit adds only benchmark tooling and evidence; the pending hotfix source remains unchanged.

## Open Questions

- Actual next release version and completion time will be supplied/verified when released; script expectedRelease is a label only.
