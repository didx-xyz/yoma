# Handoff: opportunity search follow-up — 2026-09-16

## Meta

- Feature: [opportunity search query hotfix](../feature.md)
- Session by: Adrian / Codex
- Branch: `hotfix/opportunity-search-query`
- Base: `f791d5eaf98b1fd3b8d22a380c28fdfca74671bf` (deployed production hotfix baseline; SHA verified locally)
- Previous context: [index hotfix](../../hotfix-text-search-indexes/feature.md)

## What Was Done

Prepared a local follow-up with three changes: set-based text/related matching, ID-first split hydration, and batched engagement counts for public/admin search. No schema change.

Adrian's source notes were useful, but their query totals omitted the current per-item partner-sync lookup. Do not quote the older 46-to-12 total as a complete measurement of today's endpoint.

## Worktree / Local Artifacts

- Shared checkout: `D:/source/Yoma V3`, left clean on master.
- Isolated worktree: `C:/Users/adria/AppData/Local/Temp/yoma-search-followup-worktree`.
- Disposable test harness: `C:/Users/adria/AppData/Local/Temp/yoma-search-query-validation`.
- Harness uses real EF repositories and real service methods; reflection injects dependencies and mocks lookup/sync/treasury services. It is deliberately outside the application/test suite and must not be mistaken for production wiring validation.
- Local PostgreSQL 18.4 container: `yoma-search-query-check`, no published ports, no external network, synthetic data only.
- Source attachments remain in Adrian's Downloads: `opportunity-search-performance.md` and the four `prod-*.sql` files. They were read, not executed against a remote database.

## Validation

- Whole solution build: passing with zero warnings/errors.
- Touched-file `dotnet format whitespace --verify-no-changes`: passing.
- `git diff --check`: passing.
- Small PostgreSQL fixture: 240 opportunities, active/inactive organizations, active/future/expired/inactive opportunities, hidden rows, null fields and mixed engagement actions/statuses/dates.
- 2,915 assertions passed: original predicate vs candidate counts and ordered pages across 12 terms and 16 pages; complete projected-model serialization; empty/final pages; related organization/type/category/skill matches and overlapping matches; batch counts vs existing per-item Search; duplicate IDs/empty batch; public/admin DTOs vs unchanged single-item path; most-viewed/most-completed; count-only and unpaginated modes.
- The public service path generated 11 database commands for a populated page and two for the empty search in this harness. Lookup/sync/treasury services are mocked, so these are **not** whole-endpoint deployed command totals.
- Large fixture: 30,240 opportunities, long synthetic descriptions, country links, real EF/index definitions.
- Final large-fixture run also compared total count and ordered first-page IDs directly against the original predicate for all three terms, three candidate runs each: all nine comparisons passed.

### Local synthetic timing evidence

The baseline uses the original text/related predicate plus count and full paged split projection. The candidate uses the real changed OpportunityService.Search. These timings exclude real organization lookup, sync, treasury, network and HTTP work; first-run compilation/cache costs vary.

| Search | Original count + page | Final candidate, warm runs | Matches |
| --- | ---: | ---: | ---: |
| customer hold | 19,989–20,872 ms | 10–19 ms | 0 |
| environmental | 89,276–98,766 ms | 283–401 ms | 389 |
| related skill fixture term | 93,830 ms (one run) | 65–103 ms | 14 |

An intermediate guards-plus-paging draft still took 24,163 ms for the related-skill case. That is why the final candidate separates matching sources with SQL union. Do not present the local table as Production performance or an SLA.

## Next Steps

1. Review the small domain diff. No schema/migration/config/frontend changes should be included.
2. Follow the same hotfix flow: take the reviewed patch onto a `hotfix-pr/...` branch based on master; PR to master and deploy/test Stage.
3. After Stage succeeds, cherry-pick the reviewed merged fix onto the then-current production `current` branch. Do not merge master into current: master contains CF/cash-out changes outside this hotfix.
4. Push/release only with Adrian/Robbie. Nothing was fetched, pushed, merged remotely or released in this session.
5. Verify the actual public API request with the two supplied countries and `customer hold`, then matching text, related lookup terms, empty/no-term filters, pages, and popularity filters. Compare result bodies and endpoint timings, not just isolated SQL.
6. If still slow, capture complete endpoint SQL and separately measure the remaining partner-sync lookups and writer/replica behavior.

## Gotchas / Open Questions

- Existing pending verification uses MyOpportunity.DateStart, not OpportunityDateStart. Intentionally unchanged.
- Read consistency across count/ID selection/hydration is not a transaction-wide snapshot. Concurrent deletion is tolerated; concurrent status/hidden/organization changes may be observed during hydration.
- No concurrent-write regression or full HTTP/authorization/provider integration test was performed.
- Large public page sizes and exact TotalCount remain unchanged.
- Existing indexes remain important; this patch makes their use possible in the full search rather than merely demonstrating standalone index availability.
