# Handoff: Partner profile validation hotfix — 2026-09-14

## Meta

- **Feature**: Partner navigation / profile-completion hotfix (ticket not supplied)
- **Date**: 2026-09-14
- **Session by**: Adrian (API); Jason owns the parallel web changes
- **Agent used**: Codex
- **Branch**: `hotfix/profile-completion-gate`; API changes remain uncommitted alongside Jason's committed web changes.
- **Previous handoff**: none

## What Was Done

- Investigated production Alison registration failure caused by a missing first name.
- Initial account creation legitimately permits incomplete onboarding data. The defect is allowing a required partner hand-off without the necessary profile fields.
- Added partner-specific profile validation in `src/api/src/domain/Yoma.Core.Domain/PartnerSync/Services/SyncUserAuthenticationService.cs`, before provider construction or authentication.
- Alison: email-based registration (no existing partner link) requires first name, surname and a resolvable country. Already-linked login retains its email-only contract.
- IXO: email-based hand-off requires first name, surname and a resolvable country, even for an existing link.
- Empty/whitespace email retains the existing provider-owned mobile-only redirect behaviour. Partners without user-authentication capability and unrelated partner contracts are unchanged.
- FluentValidation exceptions now propagate instead of being swallowed by the best-effort URL fallback. Other provider failures retain the existing fallback.
- Added isolated mocked service regression tests; no live partner requests or database writes are performed by those tests.
- Added derived `SyncInfoUserPartner.IsLinked`, excluded from Newtonsoft.Json serialization using the existing serializer convention. Both Alison flow selection and the new validation use it; it has no independently stored state and does not prove an account exists or does not exist at the partner.

## API / UI Contract

- Endpoint: `PUT /api/v3/myOpportunity/action/{opportunityId}/navigateExternalLink`.
- Existing exception middleware returns HTTP 400 with an array of `{ type: "ValidationException", message: "..." }` objects, one per missing field.
- Example message: `Please complete your Yoma profile: first name is required to continue to Alison.`
- ValidationFailure property names are FirstName, Surname and Country internally; the current public error response exposes messages, not property names. No new response schema or dependency was added.
- Jason's UI now prompts completion when the loaded profile is incomplete, fetching the profile first if necessary. API errors (including validation HTTP 400) close the blank tab, display the error and stop navigation instead of redirecting to the raw URL. The API-error catch itself does not reopen the completion modal; the user retries after correcting their profile.
- API partner validation is NOT a replacement for general Yoma onboarding (including gender/DOB), which remains a UI requirement.

## Jason's UI Fixes — Implemented and Source-Reviewed

- `src/web/src/components/Global.tsx`: postLoginChecks returns whether checks ran. The one-time latch is consumed only when evaluated, and route changes trigger another check after leaving exempt profile/settings/referral routes.
- `src/web/src/components/Opportunity/OpportunityPublicDetails.tsx`: checks profile completeness before proceeding, loads the profile if missing, and requests the completion modal through the shared atom. It no longer silently redirects after an API error.
- `src/web/src/lib/store.tsx`: adds profileCompletionRequestedAtom for the global completion prompt; Global consumes and clears it, including on logout.
- Reviewed these committed UI changes alongside the local API changes on the shared branch. Browser/runtime validation is still outstanding; source review is not an end-to-end test.
- The actual affected user's browser journey has not been established. These findings are based on source inspection, not a browser reproduction.

## Current State

- Build: API/test project build passed with zero warnings and errors after the final IsLinked rename.
- Formatting: `dotnet format --verify-no-changes --no-restore` passed for all four changed C# files.
- Tests: all 15 focused regression cases passed (zero failed/skipped) using `dotnet src/test/Yoma.Core.Test/bin/Debug/net10.0/Yoma.Core.Test.dll -filterVSTest "FullyQualifiedName~SyncUserAuthenticationServiceTests" -noLogo` from `src/api`.
- Migrations: none. No user data repair, Helm or configuration changes. This session changed API/test files and this handover only; Jason's three web files are already committed on the same branch.
- No commits or pushes authorised for this work yet; retain changes for the shared branch.

## Next Steps

1. Commit the API changes, tests and handover when authorised, and create/update the single shared PR. Branch transfer completed without conflicts; no stash was needed.
2. Run combined staging checks: incomplete profile after an exempt route, opportunity-login flow, slow profile loading, completion and successful retry, and an API validation error with no raw-URL redirect. Confirm existing linked Alison login and mobile-only fallback remain compatible with the general UI onboarding gate.
3. Jason should confirm web build/lint checks. API build, focused tests and formatting have passed. Release the combined PR only after staging confirmation.

## Gotchas

- Existing MyOpportunity navigation-attempt recording still happens before partner authentication. A failed hand-off may therefore leave a navigation action; it does not mean the hand-off or registration succeeded. This hotfix does not change tracking semantics.
- Do not require full onboarding fields for every partner in the backend: JobJack/Jobberman plain redirects and existing supported fallback behaviour must not acquire unintended requirements.
- The current .NET 10 / xUnit v3 setup rejects legacy `dotnet test` VSTest invocation. Use the test executable's native runner without changing repository toolchain settings for this hotfix.

## Open Questions

- Shared branch is `hotfix/profile-completion-gate`; PR details are pending.
- Combined browser reproduction and production rollout confirmation remain outstanding.
