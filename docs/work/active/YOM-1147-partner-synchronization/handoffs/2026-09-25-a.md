# Handoff: Shared skill qualifier matching — 2026-09-25

## Meta

- **Epic**: [YOM-1147](../README.md); shared follow-up arising from Umuzi Stage testing.
- **Session by**: Adrian / Codex
- **Branch**: `fix/skill-qualifier-matching`, from clean, up-to-date master.
- **Previous handoff**: [2026-09-17-a.md](2026-09-17-a.md)

## What Was Done

- Stage evidence showed Programming resolving to Programming (Music). The earlier
  fix still removed arbitrary parenthetical qualifiers to produce base-name aliases.
  A unique candidate is not necessarily semantically equivalent.
- Removed generic qualifier stripping. Full names, case/format normalization,
  exact-match precedence and ambiguity rejection remain unchanged.
- Adrian requested best useful matching across frameworks, not merely fewer matches.
  Added one shared explicit canonical-name-to-alias map for Python, JavaScript, SQL,
  Git, Django and CSS. These known aliases are registered only when the precise target
  exists in the local catalogue and use the same collision detection as other aliases.
- No Programming alias is guessed. A canonical Programming entry can still match;
  Programming (Music) continues to match when explicitly requested. New aliases must
  be reviewed for semantic equivalence, not derived from arbitrary qualifiers.
- Expanded tests cover cached/uncached behaviour, unrelated and embedded qualifiers,
  known aliases, exact-name priority, collisions, duplicate canonical targets and
  the original false positives. Shared callers remain Alison, IXO and Umuzi.

## Current State

- Build: passed through the test-project executable build, including API dependencies.
- Tests: 48 passed, zero failed/skipped, using:
  `dotnet run --project test/Yoma.Core.Test --no-restore -- -namespace Yoma.Core.Test.PartnerSync -namespace Yoma.Core.Test.Lookups -noColor`
- `git diff --check`: passed. Full test suite and live sync not run.
- Migrations/configuration/Web changes: none.
- Uncommitted: service, tests, epic note and this handover. Nothing committed or pushed.
- No Linear changes, per Adrian's instruction.

## Next Steps / Gotchas

1. Adrian reviews before commit/PR; planned normal release, not a production hotfix.
2. On Stage, rerun Umuzi opportunity pull and inspect JavaScript Fundamentals:
   Programming (Music) should disappear unless explicitly supplied by the source.
3. Verify Git/JavaScript/Python/SQL/Django remain mapped to their intended canonical
   targets, and check Alison/IXO affected examples before production rollout.
4. Arbitrary base-name matches not in the explicit alias map now remain unmatched.
   This deliberately trades speculative coverage for accuracy; it is not semantic AI.
5. Mapped payload hashes can change even without partner feed changes. Normal pull
   updates should realign opportunity skill lists. No historical credential repair.

## Umuzi Testing Context

User-provided Stage extracts on September 25 confirm all five current opportunities
imported after resetting the exhausted JavaScript retry. Learner-page navigation
with yomaUserId was demonstrated. Partner capture confirmation and live progress /
completion / credential tests remain outstanding; do not claim E2E sign-off.
