# Umuzi master merge and paging audit — 2026-09-17

## Latest state: Robbie rebase reconciled

The historical merge notes below are superseded. Local `feature/sync-umuzi` now tracks `origin/feature/sync-umuzi` at `2aad02546b76d909a8ce33b78d06036ebdec571c`; latest master `c3469ca27` is an ancestor. No merge remains in progress. The old local branch is preserved as `archive/umuzi-before-robbie-rebase-20260917`, and local edits were backed up under the temporary directory `yoma-umuzi-preserve-20260917-094344` before reconciliation.

Reviewed Robbie's `583c15577` adaptation: client, factory and keyed DI consistently use `OpportunityRequestCreate`, matching the sync consumer. Lookup collections map to IDs; active requests use PostAsActive; identity-only tombstones use the existing deletion path. Retained these changes: the earlier property-only compile fix was insufficient for runtime provider resolution and must not be restored. EF packages already match master at 10.0.12. SOPS resolution is Robbie's; no secret edits or decryption performed here.

Reapplied only the two shared Page-helper changes. Restored missing Umuzi x64/x86 solution mappings to Any CPU, consistent with master. No new Umuzi search indexes or separate child hydration are needed for its flat catalogue. Live partner pagination and sync state handling are unchanged.

Validation on the reconciled source: full API solution build passed with zero warnings/errors; all 29 focused Core regression tests passed. Partner end-to-end validation remains pending. Changes are local and uncommitted; nothing pushed.

Still pending: verify the five payout-search indexes from PR #1940 on Stage (migration `20260917061729_ApplicationDb_Payout_Search_Indexes`): Entity.User Email/PhoneNumber/DisplayName Lower_Trgm, and Payout.Transaction TransactionId/ErrorReason Lower_Trgm. Do not mark this Stage check complete without database evidence.

## Historical pre-rebase notes (superseded)

## State

Branch `feature/sync-umuzi`; local merge in progress. HEAD `04f7f2fcbad313fd9f1859f7a32b8defe65c7512`, MERGE_HEAD `c3469ca27b80ff0d147963ab3e61d882351c845f` (master including PR #1940). No commit or push made. Do not close PR #1929.

## Completed locally

- Resolved four non-secret conflicts: solution and Domain/AmazonS3/Twilio project files. Retained master's package versions and both sets of solution entries. Added Umuzi's mappings for master's x64/x86 configurations, targeting Any CPU consistently.
- Aligned Umuzi EF Core/Design/Relational to master's 10.0.12 to resolve package downgrades.
- Adapted opportunity mapping to master's enum Type using the already validated provider type; removed assignments to obsolete YomaReward/YomaRewardPool properties, which were null previously.
- Reused shared Page helper for database catalogue paging and in-memory embedded verification paging. Same guards, count timing, order, offsets, limits and mappings.
- No Umuzi ValueContains/ILIKE search or split child projection found. No new Umuzi index, migration or ID-hydration query is justified. Shared domain optimizations arrive via master. Live partner pagination/checkpoints/windows are unchanged.

## Validation

- Full API solution build passed: zero warnings/errors.
- Focused Core suite passed: 29 tests, including shared pagination and repository tests. This is not an Umuzi end-to-end partner test.
- No conflict markers remain in src/api; source diff whitespace checks pass.
- No partner API, Stage/Production database or sync job was invoked. No encrypted values were printed, decrypted or manually combined.

## Blocker and next steps

Only `helm/yoma-api/conf/base/secrets.yaml` remains unmerged. Git produced conflict markers during the requested merge; the agent made no manual edits to the file. Robbie must supply a valid combined SOPS file preserving both master's configuration and Umuzi configuration, with valid MAC. Do not select one side wholesale or ignore MAC validation.

The four Git conflict resolutions are staged. Umuzi compatibility/paging edits remain unstaged for review. A merge cannot be committed/pushed while the encrypted file is unmerged, so the remote PR still shows its original conflicts until coordination completes.

If Robbie updates the remote branch independently, fetch and inspect it before continuing; preserve the local work and reconcile the branch histories rather than force-pushing. After valid SOPS resolution: stage the intended changes, finish the merge, rerun checks and push the existing Umuzi branch. Partner readiness blockers from feature.md remain.
