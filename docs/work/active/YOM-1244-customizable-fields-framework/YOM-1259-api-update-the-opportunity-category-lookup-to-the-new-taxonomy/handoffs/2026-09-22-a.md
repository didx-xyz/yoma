# Handoff: Taxonomy seeding and migration — 2026-09-22

## Meta

- Feature: YOM-1259 — Opportunity taxonomy
- Epic: [YOM-1244](../../README.md)
- Date: 2026-09-22
- Session by: Adrian
- Agent used: Codex
- Branch: feature/cf-implementation
- Previous handoff: epic handoffs/2026-09-22-a.md (branch/icon preparation)

## What Was Done

- Scaffolded 20260922063352_ApplicationDb_CF_Configuration using EF. No schema/model change; the existing model snapshot is unchanged.
- Main migration calls two focused seeders: OpportunityCategories (guard, locks, seven renames and eight additions) and OpportunityCategoryMappings (two merges, duplicate elimination, uncategorized fallback). Extend with later CF seeders only while undeployed.
- Exact sixteen names, stable IDs and mapping are in feature.md. Other ID/image stays unchanged. Retained row dates and surviving association IDs/dates stay unchanged.
- Guard requires the approved ten legacy IDs/names before mutation. Entire migration stays transactional with NOWAIT write locks; unexpected data or failures roll back.
- Down deliberately refuses a lossy reversal. A many-to-one merge has no remaining provenance for splitting back; use a reviewed forward fix or coordinated pre-migration backup recovery.
- Verified CSV uses the lookup name resolver, preserving its case/trim tolerance but adding no old-name aliases. The new lookup makes old names invalid.
- S3 cleanup remains a TODO in OpportunityCategoryService, not a deployment action.

## Current State

- Build: full API solution passed, zero warnings/errors.
- Formatting: full solution dotnet format --no-restore --verify-no-changes passed after the final edits; git diff --check passed. The temporary PostgreSQL test server was stopped after verification.
- Tests: six focused tests passed, zero skipped, against an isolated temporary PostgreSQL 18 instance. Cases: EF operation shape, all 1,024 combinations, unexpected extra/renamed categories, no opportunities, and rollback following a deliberate late failure. Exact names/icons/IDs, retained dates, link identities, FK/unique constraints and CSV name resolution checked.
- Migrations: only actual operations executed in disposable test fixtures; no application, Stage or Production database changed. Full existing migration-chain deployment was not exercised.
- Uncommitted work: migration/designer/two seeders, migration tests, service TODO and work docs. Nothing committed or pushed.

## Next Steps

1. Update and test partner category dictionaries before deploying: Alison, Jobberman, JobJack, IXO and Umuzi; audit remaining providers. Several still reference the two removed IDs. Do not deploy this intermediate branch.
2. Jason: OpportunityImport.tsx hardcodes legacy category names in its help. Replace with final names, preferably sourced from the existing category lookup. Refresh UI selections/preset references using retired IDs. No response-shape change or new custom-field behavior.
3. Run full migration-chain and Stage CSV/API/UI/search regressions before release. The new taxonomy remains core multi-select, not a CF field.
4. Deploy with writers/syncs paused and all API instances restarted; prevent old app instances/cached old category IDs from being used during migration. Backup first; do not assume an application-only rollback restores old taxonomy.

## Gotchas

- Tests use YOMA_TAXONOMY_TEST_CONNECTION, permit only localhost/127.0.0.1 and a yoma_taxonomy_test_* database, and roll back their fixtures. With no explicit connection, database tests skip rather than use an application database.
- Legacy data associations follow the approved broad mapping; do not guess retroactive subdivisions into new narrower categories. Partner mappings can use more specific source vocabulary in the next step.
- Historical credentials/audit JSON are unchanged. Shared S3 old icons cannot be deleted until every environment migrates and rollback no longer needs them; retain Other.svg permanently.

## Open Questions

- Partner mapping refinements and web regression remain, rather than claiming the taxonomy feature ready to deploy.
