# Handoff: Discovery Surface — Design-Refinement Round — 2026-09-05 (a)

## Meta

- **Feature**: [YOM-1262](../YOM-1262-ui-apply-user-presets-to-opportunity-discovery/feature.md)
  (primary) and [YOM-1261](../YOM-1261-ui-manage-user-presets/feature.md)
- **Epic**: [YOM-1244](../README.md)
- **Date**: 2026-09-05
- **Session by**: Jason
- **Agent used**: Claude Code
- **Branch**: `feature/custom-fields-framework`
- **Previous handoff**: [`2026-09-03-b.md`](./2026-09-03-b.md)
- **Brief**: `design/2026-08-27-opportunity-discovery/2026-09-05-refinement-build-brief.md` —
  a claude.design review of the running surface. The `design/` folder is deliberately NOT
  committed; the brief's calls are recorded in the two feature docs' Decisions logs.
- **Commit shape**: two new commits on top of `89926630`, no amend — the brief's build, then
  Jason's review of it. The changes are experimental feedback and must stay revertible without
  touching the shipped rounds 2–7.

## API asks for Adrian (collected first, per the brief)

Unchanged from [`2026-08-27-c.md`](./2026-08-27-c.md): a public sort enum, the commitment null
rule (the API excludes opportunities with no commitment; the BA sheet says include), a public
`TotalCountOnly`, and confirmation that YOM-1258 makes `ApplyUserPresets` the real preset→filter
path. New from this round:

1. **A definition needs to say which type owns it.** The definitions endpoint, asked about one
   type, returns the generic definitions plus that type's own with nothing distinguishing them.
   Web now recovers the generic set by intersecting the keys returned for every selected type,
   which is inference, not fact: two types that happen to share a type-specific definition would
   have it filed under "Details (all types)". A flag (or a `types[]` on the definition) would
   make it exact.
2. **Category facet counts are total-only in local data.** All ten categories return 2 457 — the
   fixture puts every opportunity in every category, so this is a data artefact and the web
   change is only the removed caption. Worth a look at DEV data once the API redeploys: if real
   data also returns the grand total per category, the counts have to come out of the tiles.
3. (Deferred, for the card work the brief parked — recorded so it is not lost) an `isFeatured` /
   `featuredUntil` field on `OpportunityInfo`, and a per-opportunity **custom-field summary** in
   search results (`{key, title, value}[]`, ≤3) so cards can show type facts without a second
   request.

## What Was Done

Brief order: defects, wording, design consistency, layout. Section 5 (richer cards) is marked
"ignore for this session" in the brief and was not built; both §6 items marked **Build** were.

### §1 Defects

1. **Master preferences switch off now clears custom-field clauses** — the one type-shrinking
   path round 3 left open (`reduceDiscovery`'s global post-processor now also fires on
   `setPreferencesOff {off: true}`).
2. **Fetch failures are visible everywhere they can happen.** `useDiscoveryLookups` gained
   `failed` / `typesFailed` / `retry`, `useDiscoveryResults` gained `failed` / `retry`,
   `useResultCount` gained `failed` (exposed as `countFailed` on the context). Rendered as: an
   error `<Message>` + Retry in the results region; a second, quieter one when only some lookups
   failed; "Couldn't load opportunity types — retry" in place of an empty type row; a per-section
   error when the definitions call fails; "Show results" (no number, no shimmer) on the footer
   button; "Count unavailable right now" beside Clear all; and, in the wizard, "Couldn't count
   matches right now — your answers still save".
3. **`yoma.discovery.personalizationSeen` moved to `localStorage`** for both scopes (it was
   `sessionStorage` for anonymous, so every new tab re-opened the wizard).
4. **The mocked/live switch left the preference banner** for a fixed dev-tools corner
   (`PreferencesMockDevTool`), same gate as before.
5. **Generic definitions are no longer duplicated across per-type sections.** New
   `useTypeDefinitions` (a `useQueries` over the same cache keys as
   `useOpportunityCustomFieldDefinitionsQuery`) intersects definition keys across the selected
   types; the shared set renders once as "Details (all types)" above the per-type sections, which
   keep only their difference. One type selected behaves exactly as before.
6. **Category counts**: verified as a fixture artefact against the local API (see API asks) — no
   code change to the counts; the false caption was removed.
7. **"Up to a hour" → "Up to an hour"** via `upToIntervalLabel` in `lib/format.ts`, shared by the
   How-long section and the wizard.

### §2 Wording

"Save to profile" → **"Make this my default"** with an inline "Saved. Undo"; the write-back offer
merged into the preference banner as its second line; the banner names every inherited value
(two, then "+N"); the hero's results sub-line stopped pointing "above the results" at chips that
are below it; the sheet's duplicate header Clear removed; the category caption removed; wizard
step 4 re-worded; the zero-results state rewritten (below).

### §3 Design consistency

One `Badge` (three intents — amber for availability only, grey for consent, purple for
provenance); one `SectionHeader` shared by every block in the panel; the type row given a value
column and its question demoted to a subtitle; helper text as 13px subtitles with boxed callouts
reserved for null-rule warnings; 44px touch targets below `md` (custom-field controls via a new,
default-off `largeTouchTargets` prop on `CustomFieldFilters`); `IoCloseCircle` → the outline
variant (the only filled glyphs left); one loading treatment — every shimmer and the count
spinner removed; 24px rhythm in the panel, 32px on the page (24px below `md`).

### §4 Layout

Banner stack merged (above); mobile category tiles one line, no count, no square aspect
(**reverted in the follow-up below**); sub-group headings fold into the field label when the
sub-group holds one field, and entirely
below `md` (`useIsCompact`); indentation capped at one level; quick searches ordered
available-first; recent searches show a relative time; the sort row collapses to one disabled
"More sorts soon" pill below `md`.

### §6 (the two items marked Build)

**Relax-a-filter empty state** — "No matches. Try removing a filter:" followed by the applied
chips, inline and removable (the same `AppliedChips`); with nothing removable it says so instead.
**Copy link** on the results heading — the URL is already the whole search.

### New files

`components/shared/Badge.tsx`, `components/shared/CopyLinkButton.tsx`,
`components/shared/PreferencesMockDevTool.tsx`, `components/Filters/SectionHeader.tsx`,
`state/useTypeDefinitions.ts`, `state/useIsCompact.ts`.

### One fix outside the brief

`next.config.mjs` — round 6's `disable: NODE_ENV === "development"` on next-pwa is a real option
that its published type omits, so `tsc --noEmit` failed on it repo-wide (introduced by
`89926630`). The options object is now built loosely and cast at the call; behaviour unchanged.

### Follow-up, same session (Jason's review of the round — a second commit)

1. **Mobile category tiles reverted** to the shared card as it stands (square, with the count).
   The compact variant's ~45px of fold was not worth making one surface's tiles differ from
   every other one's.
2. **"Clear all" → "Clear filters", and it clears filters only** — reversing round 2's reading
   that it should also switch off the inherited layer. Reducer action `clearFilters` (no
   payload); `preferencesOff` and `preferencesSkipped` are untouched, so the inherited chips
   survive un-struck and no `prefsSkip` enters the URL. Renamed through the context
   (`clearAll` → `clearFilters`) and `CountFooter`'s prop. **It is also hidden unless the search
   carries a filter of its own** (`hasActiveFilters(state.filters)` → `hasFilters` on the
   context) — in both homes, and keyed to the manual filters rather than to the chips, because a
   row of purely inherited chips gives it nothing to do.
3. **The hero's quick-search row scrolls instead of overflowing the page.** As a flex item under
   `justify-center` the `ScrollableContainer` sized itself to its content, so it never scrolled:
   measured at 768/900/1100px the row was 1322px wide with its first badge at x=-277 and the
   document scrolling sideways to 1045/1111/1211px. The wrapper is now a block (`hidden min-w-0
md:block`) and the row uses `justify-center-safe`.

## Verified

`tsc --noEmit`, `eslint` and `prettier --check` clean across `src/features/discovery`,
`CustomFieldFilters.tsx` and `next.config.mjs` (mise node 24).

On screen, headless Chrome against `localhost:3000` + the local API on `:5000` (puppeteer-core +
chrome-headless-shell in `/tmp/yoma-harness`, not in the repo; the cached zip from the 2026-09-03
session was truncated and was re-fetched with `@puppeteer/browsers`):

- **Definition de-duplication**: Job + Event → "Details (all types) · 9 filters", "Job filters ·
  4 filters", "Event filters · 2 filters" (was 13 + 11 with nine drawn twice). One type → a
  single "Job filters · 13 filters" section, unchanged.
- **Sub-group folding**: desktop keeps the two-field "APPLICATION" heading and folds the five
  one-field sub-groups into labels ("Basics · [Sample] Audience Description"); at 390px there are
  no sub-group headings at all and every field carries the prefix.
- **Master switch**: `?type=Job&cf=[…]` → toggle off → `?type=Job&prefsOff=1`, clauses gone.
- **Failure states**, each by aborting one request pattern (react-query retries three times, so
  they take ~15s to appear): search → "Couldn't load these results. Retry"; `/opportunity/type` →
  the type-row one-liner AND the footer reading "Show results"; `filter/category` → "Some filter
  options couldn't be loaded, so this search may be incomplete. Retry"; `custom/field/definition`
  → "Couldn't load the filters for this type. Retry".
- **Make this my default → Undo**: skip the inherited Goal → the offer appears inside the banner
  → save writes `goal: null` to the store and clears `?prefsSkip=goal` → "Saved. Undo" → Undo
  restores `goal: "job"` and `?prefsSkip=goal`. Verified in both directions, including the case
  where saving empties the query and the surface flips from results to landing.
- **Banner line**: "Your feed is tuned to Job · AI, Data and Analytics" (was "…to Job").
- **Empty state**: `?q=zzzzqqqnothing&type=Job` → "0 matches for “zzzzqqqnothing” + 1 filter",
  then "No matches. Try removing a filter:" with a removable `Type: Job` chip.
- **The follow-up three**: the hero row now scrolls at 768 / 1100 / 1440 with its first badge
  inside the content box and `document.scrollWidth === window.innerWidth` at each (it was
  1045 / 1111 / 1211 against 768 / 900 / 1100 before); "Clear filters" on a search with a
  preference applied empties the URL and leaves both inherited chips on screen, un-struck, with
  no `prefsSkip`; the category tiles are square with counts again. The clear action is absent on
  a clean landing AND with preferences applied but no filters (inherited chips on screen, no
  clear button anywhere), and present in all three homes once `?q=` is set; with it hidden the
  footer's Show button still sits 12px off the panel's right edge.
- **Mobile (390×844)**: chips wrap to two lines instead of clipping; the sort row shows only
  "More sorts soon" (the two inert pills are display:none); quick-search badges and type pills
  measure 44px in the sheet, 36px at 1440, and the custom-field operator selects and value inputs
  44px in the sheet against 40px at 1440. (The fold measurements taken with the compact category
  tiles no longer apply — those tiles were reverted, see the follow-up.)
- **Wizard**: step 4 reads "The most time you can give…" and its pills read "Up to an hour".
- **Recents**: "analytics | 2457 results · just now" in the Search popover.

Not verified on screen: the recents panel inside the MOBILE sheet (headless focus is stolen from
the input the moment it is focused, so the typeahead never opens there — it is the same component
the desktop popover renders, which was verified). A human pass on a real phone is still owed.

## Current State

- Build: `tsc --noEmit` clean (and it was NOT before this session — see the `next.config.mjs`
  note). Lint and format clean. `src/web` still has no test runner, so the breakpoint-parity
  assertion remains manual (2026-08-27 decision unchanged).
- Uncommitted work: none. The round is TWO commits — the brief's build, then Jason's review of it
  (the follow-up three) — so either can be reverted on its own. The `design/` folder (the brief
  and its PNG exports) stays out of the repo by design.

## Next Steps

1. Jason: visual pass at 1440 and 390 over this round, especially the merged banner, the
   "Details (all types)" split with three or more types selected, and the sheet's recents.
2. Decide whether the brief's §5 (richer, type-aware cards) becomes its own session — the API
   asks it depends on are listed above.
3. If any of this round is unwanted, revert its commits; rounds 2–7 are untouched by them.
4. Blocked items unchanged: live preset data (YOM-1257 / YOM-1258), the final field set
   (YOM-1264), and the DEV API redeploy.

## Gotchas

- **The de-duplication is an intersection, not a fact.** With two or more types selected, a
  definition that all of them return is filed under "Details (all types)" even if it is
  type-specific to each — the endpoint gives us nothing better (API ask #1). With one type
  selected nothing is intersected.
- **`useTypeDefinitions` deliberately re-declares the query key and fetcher** of
  `useOpportunityCustomFieldDefinitionsQuery` (hooks cannot be called in a loop; `useQueries`
  can). If that hook's key ever changes, this one must change with it or the two will fetch the
  same definitions into two cache entries.
- **The banner's undo snapshot lives on the context, not in the banner.** Saving the last
  override can empty the query, which swaps `DiscoveryResults` for `DiscoveryLanding` and
  remounts the banner; component state would have taken the undo with it.
- **`useIsCompact` starts `false` and settles in an effect** (no `matchMedia` during render, or
  the server and the first client render disagree). A mobile reader therefore sees the desktop
  sub-group headings for one frame.
- **44px targets in the custom-field controls are opt-in.** `largeTouchTargets` is off by
  default so the admin and legacy filter panels are unchanged; a future caller that wants the
  bigger controls has to ask for them.
- The chip's `animate-pulse` survived the "remove every shimmer" sweep on purpose: it marks the
  newly added chip, it is not a loading placeholder.
- **A `ScrollableContainer` inside a flex parent silently stops scrolling.** It sizes to its
  content as a flex item, so `overflow-x-auto` never engages and the row overflows the page
  instead. Give the wrapper a width (`block`, or `min-w-0` plus `grow`) — the hero's quick-search
  row was broken this way between `md` and ~1350px. Worth checking the next scroller added.
- The headless harness lives in `/tmp/yoma-harness` (scratch, not the repo). Its clicks run
  through the DOM (`el.click()`), and the write-back offer needs ~10s of settle before it exists
  — several early runs looked like a broken save when the button simply was not there yet.

## Open Questions

- Per-preference vs per-value override granularity — unchanged from `2026-09-02-a.md`.
- The brief's "results above the fold at 390×844" is met for a search with preferences applied
  (first card at y=739) because the tiles gave back ~45px and the page rhythm stays at 24px below
  `md`. It is NOT met if the banner also carries the write-back offer, which adds a line. Buying
  more would mean dropping the category carousel on mobile, and that contradicts the 2026-08-27
  "carousel keeps its position" decision — flagged rather than decided.
