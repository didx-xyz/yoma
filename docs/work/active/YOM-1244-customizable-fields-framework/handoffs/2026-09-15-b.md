# Handoff: Executed migration validation and settings seed review — 2026-09-15

## Meta

- **Feature**: Cross-cutting CF, Treasury, Payout and SSI migration validation
- **Epic**: [YOM-1244](../README.md); also [YOM-1051](../../YOM-1051-zlto-yellow-card-cash-out/README.md)
- **Date**: 2026-09-15
- **Session by**: Adrian
- **Agent used**: Codex
- **Branch**: feature/custom-fields-framework
- **Previous handoff**: [2026-09-15-a.md](./2026-09-15-a.md)

## What Was Done

- Moved payout preference insertion/deletion from the consolidated migration body into its own
  `_Seeding_Settings.cs` partial helper, using SeedSettings/UnseedSettings. Eight helpers now exist;
  no setting values or migration operations changed.
- Moved the four actual payout SendGrid template IDs to ignored appsettings.local.json as requested.
  Committed appsettings.json contains [templateId] placeholders. Deployed environments must supply IDs
  through their own configuration. No Helm/configuration-service changes were made.
- Ran PostgreSQL 18.4 in a disposable Docker container with no network access, published ports or
  existing volumes. An EF-only runner executed in the same isolated network namespace. The API,
  Hangfire, email delivery and external providers were never started or called.

## Current State

- Build: API and temporary EF runners passed with zero warnings/errors.
- Clean installation: all 58 ApplicationDb migrations applied through the actual EF migration runner.
  The other contexts' 10 protected migrations were not rerun; their files remain unchanged.
- Upgrade: populated the 56-migration deployed ApplicationDb baseline using an adapted copy of the
  existing local synthetic fixture script. Adaptation only supplied non-null pre-migration credential
  versions. Dataset: 4 users, 16 organizations, 5,000 opportunities, 5,152 MyOpportunity records,
  50 referral programmes, 30 reward transactions, 12 credential issuance records and 17 settings.
- Injected known Treasury amounts, deliberately wrong participant counts, an issued credential version,
  unissued versions and saved user preference data to exercise transformations and preservation.
- All 17 SQL assertions passed: existing business rows/preferences preserved except intended columns;
  baseline history unchanged; correct setting/default/visibility/roles; renamed Treasury values and
  recalculated totals; issued version preserved/unissued versions cleared; statuses, CF seeds and
  all 16 presentation mappings; removal of YomaReward metadata; participant repair and protected indexes.
- Independent equivalence test: applied the saved four original migrations to an identical database
  clone. All 79 application-table row counts and data fingerprints match the consolidated result,
  excluding generated DateCreated/DateModified timestamps. The full application schema dumps match.
  Existing business-row timestamps were also covered by the exact preservation assertions above.
- Repeat application: EF is a data no-op; idempotent upgrade SQL reruns successfully without changing
  verified data or duplicating the payout setting.
- Rollback/reapply: clean database returned to the 56-migration baseline and reapplied both new
  migrations successfully. This does not promise restoration of deliberately removed historical data.
- Repair safety: a conflicting writer causes PostgreSQL 55P03 immediately; no participant changes or
  repair history entry are committed. Retrying after releasing the lock succeeds. Missing Completed
  lookup similarly fails the guard; restoring it permits a successful retry.
- No existing local, Dev, Stage or Production database was modified. The disposable database container
  was stopped/removed after testing; runner and SQL artifacts remain outside the repo.
- Nothing committed by this session.

## Gotchas

- Full standalone SQL export from migration zero fails in protected migration
  `20240723103044_ApplicationDb_Settings_Rework`: its raw DELETE lacks a semicolon before the
  following generated INSERT. Actual EF execution separates commands and passes. No deployed migration
  was edited. Use the normal EF startup workflow for fresh installs, not the unmodified full SQL export.
  The new baseline-to-latest idempotent SQL itself was executed successfully.
- Synthetic-data equivalence is strong coverage of consolidation, not a production-data restore test
  or full API/business-flow sign-off. Existing rollback limitations remain as documented previously.
- EF CLI 10.0.11 emits a version warning against runtime 10.0.12. Actual execution used runtime 10.0.12.

## Next Steps

1. Adrian reviews/commits. Reset existing local/Dev databases containing superseded IDs before startup.
2. Run normal local API smoke tests, then normal deployment validation. Keep Stage/Prod history intact.
3. Before participant repair on Stage/Prod, pause completion/import writers and audit totals afterwards.
4. Supply deployed SendGrid template IDs separately and verify real template rendering/delivery.

## Open Questions

- No consolidation blocker found. Production-like data restore testing and business-flow QA remain separate.
