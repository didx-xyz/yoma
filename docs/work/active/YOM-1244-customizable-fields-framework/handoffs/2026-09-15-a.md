# Handoff: Undeployed API migration consolidation — 2026-09-15

## Meta

- **Feature**: Cross-cutting CF, Treasury, Payout and SSI migration sequence
- **Epic**: [YOM-1244](../README.md); also [YOM-1051](../../YOM-1051-zlto-yellow-card-cash-out/README.md)
- **Date**: 2026-09-15
- **Session by**: Adrian
- **Agent used**: Codex
- **Branch**: feature/custom-fields-framework
- **Previous handoff**: 2026-09-05-a.md (epic); migration-specific predecessors are 2026-08-14-a.md and YOM-1254/handoffs/2026-09-07-a.md.

## What Was Done

Adrian supplied Stage and Production EF history exports and approved rewriting only migrations
absent from both. Both exports contain the same 66 migration IDs, matching origin/master.
Historical ProductVersion differences do not change that protected set. The branch includes the
fetched master baseline; none of the 160 deployed migration source/designer/seed files was changed.

The following three undeployed migrations are consolidated into
`20260915100000_ApplicationDb_Custom_Fields_Treasury_Payout_SSI`:

- `20260806191303_ApplicationDb_Custom_Fields_Treasury_Payout`
- `20260814101500_ApplicationDb_SSI_Credential_Schema`
- `20260915080423_ApplicationDb_Settings_User_Payout_Notifications`

All original Up statements remain in that order; Down retains the reverse order. All seven seed
helpers retain their bodies under the consolidated partial-class name and file prefix. This preserves
CF definitions/options, Opportunity type display names, Treasury recalculation, payout/reward statuses,
Yoma reward removal, credential issuance updates, schema presentation metadata and the notification setting.
Intermediate schema operations are deliberately retained rather than introducing a second optimization.

`20260907114930_ApplicationDb_Opportunity_ParticipantCount_Reconcile` is now
`20260915100001_ApplicationDb_Opportunity_ParticipantCount_Reconcile`, immediately afterwards.
Its implementation is byte-identical: NOWAIT locks, lookup guards, count correction and intentionally
empty Down are preserved. Its designer ID changed; its model did not.

The consolidated designer carries the final model. ApplicationDbContextModelSnapshot is byte-identical.
No Umuzi integration was included. Notification code and other pending work were preserved.

## Current State

- Build: API build passed with zero warnings/errors.
- Source checks: consolidated Up/Down match the concatenated originals, ignoring indentation and
  renamed helper identifiers; all seven helper bodies are preserved; repair and snapshot hashes match.
- SQL checks: generated forward and rollback SQL matches the original operations, including data
  transformations. Comparison excludes history records, transaction wrappers and existing generated
  DateTimeOffset.UtcNow seed timestamps. Protected interleaved migrations generate unchanged SQL.
- Offline SQL generation passed for both a fresh ApplicationDb install and an idempotent upgrade from
  `20260825090429_ApplicationDb_Query_Performance_Indexes` (the last deployed ApplicationDb migration).
- EF reports no pending model changes. EF CLI 10.0.11 warns that the updated runtime is 10.0.12;
  generation succeeds. No package or tool version was changed by this consolidation.
- Database execution: NOT performed. No local, Dev, Stage or Production database was modified.
- Audit copies and before/after SQL are outside the repo in the local temp directory
  `yoma-migration-consolidation-20260915`; they are not deployment artifacts.
- Uncommitted work: consolidated migration sequence, notification work and related documentation.

## Next Steps

1. Adrian reviews and commits the complete migration additions/removals with the related changes.
2. Adrian resets local and Dev before deploying this sequence. Do not run it over a database containing
   any superseded migration ID: those environments must be rebuilt as agreed, not history-edited.
3. Test a clean local application and a restored Stage-like dataset through the normal migration workflow.
   Offline SQL equivalence is not a substitute for executing against representative data.
4. For Stage/Production, preserve every existing history row. Only the two September 15 IDs are new.
   Pause completion/import writers before the participant repair; then audit participant counts and
   recorded ZLTO totals per the existing release gate. Do not deploy a master-model API against this schema.

## Gotchas

- The protected August 24/25 migrations originally sorted after the undeployed August 6/14 migrations.
  Protection was based on exact deployed IDs, not a date cutoff. Both new IDs now follow the deployed baseline.
- The repair remains a separate EF migration/transaction; never suppress its lock transaction.
- Consolidation intentionally does not improve historical rollback semantics. Dropped legacy Yoma reward
  data is not recoverable through Down, and participant counts are intentionally not reverted to wrong values.
- Historical handoffs retain their original IDs as contemporaneous records; use the mapping above for current code.

## Open Questions

- None for scope. Database execution and deployment validation remain outstanding.
