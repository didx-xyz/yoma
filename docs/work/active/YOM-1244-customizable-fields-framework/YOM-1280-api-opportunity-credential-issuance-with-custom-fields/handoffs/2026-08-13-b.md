# Handoff: Credential Issuance Runtime Verification - 2026-08-13

## Meta

- **Feature**: YOM-1280 - Opportunity credential issuance with custom fields
- **Epic**: [YOM-1244](../../README.md)
- **Date**: 2026-08-13
- **Session by**: Adrian
- **Agent used**: Codex
- **Branch**: `feature/custom-fields-framework`
- **Previous handoff**: [2026-08-13-a](./2026-08-13-a.md)

## What Was Done

- Applied the schema-metadata migration and regenerated local development data.
- Updated PostgreSQL and SQL Server development seeds so pending issuance rows leave schema version null.
- Verified tenant creation, partner synchronization and generic `Opportunity|Default` issuance against a running API.
- Verified issued rows carry a schema version matching the provider schema ID; non-issued rows do not carry a version.
- Verified ten signed JWS Opportunity credentials across local, Alison, Jobberman and IXO data.
- Verified Skills are signed as JSON name items and are currently returned as raw JSON by wallet detail.
- Made the internal issued timestamp culture-independent using invariant round-trip serialization and verified the exact value in the stored attributes, signed JWS and wallet list/detail responses.
- Aligned the newly merged JobJack pull client, factory and keyed registration with the CF branch's `OpportunityRequestCreate` pull contract. Origin master remains internally consistent on the earlier domain-model contract.

## Current State

- Build: passing - full solution Release build completed in an isolated artifacts directory with 0 warnings and 0 errors
- Tests: running API verification passed for generic first-attempt issuance and wallet retrieval
- Migrations: applied successfully to regenerated local development data
- Uncommitted work: YOM-1280 issuance mechanics, migration, seed alignment, JobJack branch compatibility and documentation

## Next Steps

1. Commit the verified issuance work.
2. Implement wallet normalization/rendering for structured and legacy Skills and dynamic custom-field attributes.
3. Exercise latest-version resolution, retry and required-value failure paths once suitable type-specific schemas and mapped custom fields exist.

## Gotchas

- Skills display is deliberately incomplete: `valueDisplay` contains raw JSON until the wallet display phase. Do not make Web parse the provider representation.
- JobJack required a CF-branch-only generic contract alignment after master was merged. No corresponding master hotfix is required.
- The issuance job captures one timestamp for the batch, so credentials issued in the same run legitimately share `dateIssued`.
- Final type-specific schema seeds and partner/import assignments remain blocked on the approved YOM-1264 matrix.

## Open Questions

- None for the generic issuance lifecycle. Type-specific and custom-field credential verification awaits the final schema matrix.
