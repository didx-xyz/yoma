# Handoff: Participant-count reconciliation — 2026-09-07

## Meta

- **Feature**: YOM-1254 — API custom-fields framework / CSV completion integrity
- **Epic**: [YOM-1244](../../README.md)
- **Date**: 2026-09-07
- **Session by**: Adrian
- **Agent used**: Codex
- **Branch**: `feature/custom-fields-framework`
- **Previous handoff**: `2026-07-08-a.md`

## What Was Done

- Confirmed the existing CSV rollback/EF-state code fix had no participant-count data migration.
- Added EF migration `20260907114930_ApplicationDb_Opportunity_ParticipantCount_Reconcile`, with generated designer metadata and no schema changes.
- Counts only Verification + Completed records. Repairs both undercounts and overcounts across all opportunity statuses and sources; no ExternalId filter. Existing null/zero counters with no completions remain unchanged, while nonzero counters with no completions become zero.
- Guards the required lookups before updating. Locks MyOpportunity then Opportunity in the migration transaction, using NOWAIT to fail safely on concurrent writers. No separate commit or suppressed transaction.
- Leaves ZLTO, limits, timestamps, credentials and completion records untouched. Down is intentionally empty rather than restoring incorrect values.
- Adrian manually repaired production: the supplied initial audit had 96 undercounts and three overcounts, with no ZLTO discrepancies. After both repairs, Adrian reported that the audit returned no rows. This migration has NOT been run against production.

## Current State

- Build: EF migration generation and SQL generation succeeded.
- Tests: exact migration SQL passed isolated PostgreSQL 17 fixtures covering missing/under/over/zero counts, exclusion of pending/rejected/navigation records, untouched rewards and idempotence (second execution updated zero rows).
- Deployment verification: generated EF idempotent SQL applied twice successfully to the disposable fixture database. EF reports no pending model changes; `git diff --check` passed.
- Migrations: pending deployment with CF; no existing migration edited, model snapshot unchanged.

## Next Steps

1. Review and deploy with CF, pausing completion/import writers for the migration. NOWAIT contention aborts safely; retry in a quiet window. Allow sufficient migration command timeout for production volume.
2. Run the production count/reward audit after deployment and exercise CSV completion imports to confirm the permanent code fix.
3. Until then, Adrian audits/repairs monthly and immediately after CSV imports when notified by Vennita. Existing monthly reminder is active; it never executes SQL.
4. Stop the reminder only after the CF production code fix and post-deployment verification are confirmed.

## Gotchas

- Manual repairs do not prevent further drift before CF deployment.
- The reproduced CSV failure is explained by EF accepting tracked values inside rolled-back validation probes. Do not claim every historical discrepancy has that proven cause; the three historical overcounts remain unexplained.
- Production's manual correction may make this migration a no-op, which is expected. Other environments still need reconciliation.
- No Web/API payload changes and no client message sent automatically.

## Open Questions

- None blocking this data migration; CF release timing remains to be confirmed.
