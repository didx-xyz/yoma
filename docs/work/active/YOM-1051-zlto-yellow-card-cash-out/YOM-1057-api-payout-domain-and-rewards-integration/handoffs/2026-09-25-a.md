# Handoff: completed cash-out wallet access — 2026-09-25

## Meta

- **Feature**: YOM-1057 — API payout domain and rewards integration
- **Epic**: [YOM-1051](../../README.md)
- **Date**: 2026-09-25
- **Session by**: Adrian
- **Agent used**: Codex
- **Branch**: `feature/payout-wallet-access`
- **Previous handoff**: `2026-09-21-d.md`

## What Was Done

- Added provider-neutral `payout.walletAvailable` and nullable `payout.walletUrl` to `GET /api/v3/user`. They are not on `GET /api/v3/user/payout/latest`, which remains for outcomes and cancellation guidance.
- Profile construction performs one user-scoped existence check for any Completed reward-funded cash-out with the configured provider. This preserves wallet access if a newer payout is pending or failed, without calling IXO.
- The payout service returns `(Available, Url)` together; the profile assigns the flag and URL directly rather than inferring the flag from a string.
- Added required, separately validated `IXO:YellowCard:WalletUrl` rather than deriving the landing URL from the API `BaseUrl`.
- Added focused tests for earlier-completed and no-completed cases, and kept cancellation behavior intact.

## Current State

- Build: passing (`dotnet build Yoma.Core.sln --no-restore -v:q`, zero warnings/errors).
- Tests: passing (`dotnet run --no-restore --project test/Yoma.Core.Test/Yoma.Core.Test.csproj -- -class Yoma.Core.Test.Core.PayoutCancellationTests -reporter quiet`).
- Migrations: none.
- Uncommitted work: API model, service/provider options, tests, configuration placeholder and these docs. Nothing pushed.

## Next Steps

1. Configure `IXO:YellowCard:WalletUrl` as the hosted landing URL for each environment. It is required at application startup. The committed `appsettings.json` value is IXO's Test URL (`https://test.zlato.offramp.yoma.ixo.earth`); Production **must override it** with its own confirmed landing URL before deployment, or the production button will open Test.
2. Jason: add `walletAvailable: boolean` and `walletUrl: string | null` to the profile payout model, and render a "View wallet" action next to Cash Out on the wallet card when availability is true. Open the URL as a top-level page so the youth can sign in and manage their wallet; do not use the payment-session URL. The action is independent of `payout.enabled` (new-initiation kill switch) and the youth's current country availability. No `GET /user/payout/latest` call is needed to render it. Do not infer wallet balance, bank delivery, or retry eligibility from this flag. Jason owns all web changes.
3. Jason: `CashOutEntry.showOutcome` currently calls `getLatestPayout()` and `refreshProfile()` concurrently. If the completion webhook lands between those reads, the outcome can say Completed while the profile still has `walletAvailable: false`. Refresh the profile after observing a Completed outcome (rather than relying only on the parallel read), so the new button appears immediately. "Check again" already calls `showOutcome` and refreshes the profile. A webhook that arrives after the youth closes the dialog still needs a subsequent profile refresh/navigation; there is no automatic provider push into the page.
4. Review and commit; verify on Stage with a completed cash-out followed by a newer non-completed one.

## Gotchas

- The URL is independent of `BaseUrl`; do not expose the API endpoint or allow the committed Test URL to be inherited in Production.
- `Completed` marks Yoma-to-wallet completion, not final bank/mobile-money delivery. The link remains useful after later attempts.
- The normal `dotnet test` invocation is rejected by this .NET 10/xUnit v3 project; use its in-process runner above.

## Open Questions

- Confirm the Production hosted-wallet landing URL with IXO before rollout.
