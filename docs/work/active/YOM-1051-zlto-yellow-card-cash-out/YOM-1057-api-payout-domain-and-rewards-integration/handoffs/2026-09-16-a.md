# Handoff: Payout environment gate — 2026-09-16

## Changes

- Added `AppSettings:PayoutEnabledEnvironments` and its parsed enum accessor using the existing environment-list parser.
- Committed appsettings value is `Staging, Production`; Local and Development cannot initiate new payouts. Missing/empty configuration parses to None.
- `IPayoutService.Enabled` supplies both the initiation guard and profile `payout.enabled`.
- Both public initiation methods reject disabled environments with FluentValidation/HTTP 400: `Cash-out is not available`. Guard runs before transaction creation, provider calls or reward reservation.
- Resume, country availability, conversion, latest outcome, webhook handling and reconciliation are unchanged. Recovery may finish initiation of an already-recorded payout; this deliberately does not abandon existing reservations.
- No migration, database reset, Helm change or Web code change.

## Jason — UI action required

- Read `profile.payout.enabled` before offering NEW cash-out from Marketplace or the wallet. Hide/disable initiation when false; do not rely solely on hiding UI because the API also enforces the gate.
- This is environment configuration, not provider health, country support, balance eligibility or an active-payout status.
- When false and `payout.active` is true, keep the existing status/recovery panel and Continue action when `canResume` is true. Do not hide the entire payout panel.
- Keep using `/user/payout/latest` for terminal outcomes. When enabled, all existing country/wallet/balance/active-payout checks still apply.
- Check Dev/Local false and Stage true after matching API/UI deployments. Production is configured true by Adrian's decision, but is not ready until real provider configuration and rollout checks are complete.

## Robbie

Add `AppSettings:PayoutEnabledEnvironments: "Staging, Production"` to deployed configuration, with no Dev override enabling Development. Verify Stage's complete IXO Yellow Card Test configuration matches Dev's working provider configuration. SendGrid templates were already applied and verified. Coordinate webhook delivery to Stage with IXO.

## Validation

- Build and final diff checks recorded in the session response; no live payout/provider requests were performed.
- Manual checks: disabled initiation returns 400 without a new transaction/reservation; profile still loads with enabled=false; active payouts remain resumable/reconcilable; Stage initiation passes this gate and continues normal validation.

## Caveats

- Disabled initiation does not remove the existing provider startup configuration requirements.
- Shared ZLTO wallets are not environment-isolated. Keep old Dev transaction recovery distinct from fresh Stage testing.
- Production in the allowlist activates initiation as soon as this code/config is deployed there. Do not deploy Production using Test credentials.
