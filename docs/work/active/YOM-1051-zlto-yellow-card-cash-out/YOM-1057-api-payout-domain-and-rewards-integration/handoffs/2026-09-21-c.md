# Handoff: IXO country minimum mapping — 2026-09-21

## Meta

- Feature: YOM-1057 — API payout integration
- Epic: [YOM-1051](../../README.md)
- Date: 2026-09-21
- Session by: Adrian
- Agent used: Codex
- Branch: feature/payout-minimum-and-cancellation
- Previous handoff: 2026-09-21-b.md

## What Was Done

- Completed Michael's confirmed countries contract. The configured public countries URL now adds limits=true, preserving ramp=offramp; no authentication or Helm changes.
- Added nullable limits dictionary and lowestMinUsd wire model. Common PayoutHelper maps it directly into MinimumAmount in USD. The provider's local currency/rate and method limits are not used for this country-wide floor.
- Zero maps to null (no minimum). Missing/null country limits also remain null by the agreed compatibility contract. Negative values fail as invalid provider data.
- No extra conversion or buffer: IXO already includes 5%. Hosted checks still enforce the selected payout method's minimum. Nullable highestMaxUsd is captured in the provider model only (null means no maximum), with no domain/UI exposure or Yoma maximum validation. Hosted per-method maximum checks remain authoritative; low current balances are not a guarantee against exceeding future limits.
- Uses CacheSlidingExpirationInHours as an absolute duration for this lookup, capped at one hour; no sliding expiration is set. Changed its key so a previous no-limits entry is not reused. Profile/initiation reuse the same lookup; no additional call per profile.
- Read-only request to the live public endpoint confirmed 17 countries and the supplied shape, including positive ZA/BW limits and zero for BJ. Values are dynamic, not hard-coded.
- Added ten focused country-mapping/cache tests. Updated the epic contract and feature checklist. Earlier handoffs' pending-mapping statements are superseded by this handoff.

## Current State

- API build: passing, zero warnings/errors.
- Focused tests: 85 passing (country mapping, minimum boundaries, cancellation and provider cancellation).
- Migrations: none.
- Uncommitted work: provider client/models/helper, country tests and handover documents. Nothing committed, pushed or deployed in this session.

## Next Steps

1. Commit/push these changes to the existing PR and let CI complete before merging.
2. On Stage, confirm countries and profile expose the same minimum/currency, and test below/equal/above the current USD minimum using the conversion preview. Below must fail before reservation; equal must pass the minimum check (other validations still apply).
3. Check a zero-limit country displays no minimum, and run the cancellation/resume smoke tests with Jason.

## Gotchas

- Jason: no response-shape changes are required. Continue reading payout.countryAvailability.minimumAmount/currency; active payout amount/currency stay independent. Compare USD, not ZLTO.
- Minimum is the lowest across methods, not a guarantee every method accepts that amount. Hosted flow remains final authority and values may change within the one-hour cache period.
- Existing payouts are not revalidated against new limits. CanCancel behavior is unchanged: session and on-demand latest-info only, not profile.

## Open Questions

- No outstanding provider minimum-field mapping questions. Stage end-to-end validation remains required.
