# Handoff: YOM-1057 — Final compact payout profile contract — 2026-09-10

## Meta

- **Feature**: YOM-1057 — Payout domain and rewards integration
- **Epic**: [YOM-1051](../../README.md)
- **Date**: 2026-09-10
- **Session by**: Adrian
- **Agent used**: Codex
- **Branch**: feature/custom-fields-framework
- **Previous handoff**: 2026-09-10-b.md (superseded before commit)

## What Was Done

- Consolidated payout status into the existing profile after Adrian's review. Removed the proposed
  latest endpoint, nested summary and duplicate historical reward amount.
- One query selects active payout first, otherwise latest terminal. Default activeOnly filtering remains
  for initiation/session guards and retains SingleOrDefault duplicate-active detection.
- Profile adds status, canResume and dateCreated, with no transaction ID or duplicated ZLTO.
- Active derives from status; retained terminal amount/currency no longer imply active.
- Wallet pending reservation lookup remains active-only. Terminal states do not add reward lookups.
- Documented exact POST initiation / GET session-refresh endpoints and iframe modal behavior for Jason.
- Preserved earlier conversion-rate and HTTP 400 amount-validation fixes.
- At Adrian's request, removed the newly added PayoutYouthContractTests.cs before commit.
  Existing tests were untouched; adding maintained automated coverage is deferred to technical-debt work.

## Current State

- Build: passing — API/test-project dependency graph, zero warnings/errors.
- Tests: 15 temporary focused tests passed during development, then were removed at Adrian's request.
  No new automated tests are included in this change. Earlier handoffs describe tests exercised at the time,
  not coverage retained in the repository. Real hosted/webhook E2E remains outstanding.
- Migrations: n-a.
- Uncommitted work: combined UI-feedback fixes and handoffs. Not deployed.

## Next Steps

1. Adrian reviews and commits; deploy before Jason consumes the new profile fields.
2. Jason follows the final shared contract in the epic README. Use profile status for Flow D,
   existing wallet ZLTO figures for balances, and the returned paymentUrl as iframe src.
3. Resume Dev hosted/webhook testing with Adrian after the next deployment/reset; IXO has replied,
   but the reply details and successful verification have not yet been recorded here.
4. Reintroduce maintained automated cash-out coverage during the planned technical-debt work.

## Gotchas

- Compatibility: payout.amount/currency now remain visible after closure. Use active/status, never
  amount presence or pendingPayout zero, to infer the outcome.
- Processing does not prove youth confirmation or fiat delivery. Use neutral in-progress wording.
- Pending ZLTO becomes zero after commit/release; do not claim it is a historical amount. Outcome copy
  can omit the exact historical reward amount and show the updated wallet.
- Do not POST a second payout to refresh its URL. GET payout/zlto refreshes the same active session.
- Closing the iframe neither cancels nor completes a payout. Refresh profile; no return URL required.
- These proposals were revised within uncommitted work; earlier handoffs are explicitly superseded.

## Open Questions

- Email-notification ownership remains separate from the in-app outcome.
- Provider authentication escaping the iframe requires IXO coordination if encountered.
