# Handoff: UI — Youth Cash-Out (Payout) — 2026-09-11

## Meta

- **Feature**: YOM-1074 — UI youth cash-out (payout)
- **Epic**: [YOM-1051](../../README.md)
- **Date**: 2026-09-11
- **Session by**: Jason
- **Agent used**: Claude Code
- **Branch**: `feature/custom-fields-framework`
- **Previous handoff**: [`2026-09-10-a.md`](./2026-09-10-a.md)
- **Built against**: Adrian's API additions of 2026-09-10 (`787920c6`, `1c409c11`, `7a0e19f2`,
  `6705baef`) and his epic-README section _Youth cash-out API additions — 2026-09-10_

## What Was Done

Adrian delivered all four API asks from yesterday's handoff, plus a directive that changes the
shape of the journey. This session wired them up, deleted the two workarounds they made
unnecessary, built Flow D for real — and found a bug that would have made Cash Out unreachable for
every youth in the product.

### The bug worth reading first

**`WalletCreationStatus` was a numeric TS enum.** The API serialises every enum as its PascalCase
name — `Startup.cs` registers a strict string enum converter, the Swagger schema says
`"type": "string"`, and the rest of this codebase already compares these fields as strings
(`item.status === "Active"`). So `walletCreationStatus` arrives as `"Created"` while
`WalletCreationStatus.Created` was `2`, and the gate's `!== WalletCreationStatus.Created` was true
for **every** profile: Cash Out would have refused everyone with "Your wallet is still being set
up". The enum was also missing `PendingUsernameUpdate`, so its ordinals were wrong regardless.

It is now a string enum mirroring the C#. Nothing about this fails to compile, and no screenshot
would have shown it — it surfaced from reading the Swagger schema while typing the new payout
status. **Numeric TS enums against this API are latent runtime bugs wherever they appear**; that is
now recorded in the epic's Cross-Area Notes, because it is not a payout problem.

### Wired up (all verified against the code and the Swagger schema, not the message)

- **`profile.payout`** → `{ countryAvailability, active, status, canResume, amount, currency,
  dateCreated }`, active payouts only. The resume panel gained **Started**, and the "still setting
  up" state is now known when the dialog opens rather than discovered by tapping a button that
  cannot work yet.
- **`GET /user/payout/latest`** → `PayoutTransactionInfo`, 404 when the youth has no payout.
  `api/services/payout.ts` takes the 404 as data, like the 503 on `/countries`.
- **`conversionRateZltoPerUsd`** on the conversion preview. **The inference is deleted** —
  `lib/payout/conversion.ts` is now a formatter. The rate line is exact for every amount, including
  the small ones the old interval arithmetic had to hide from.
- **Amount rules are 400s now** (`ValidationException`), so "must be greater than zero" and "must be
  a whole number" land on the field instead of becoming a generic failure. The client guards stay.

### Built

- **The hosted journey is an iframe inside Yoma** (`CashOutHostedStep.tsx`), not a new tab. The
  dialog grows to `md:h-[85vh] md:w-[720px]` for that step only. No DOM inspection, no URL watching,
  no `postMessage` listener — none of those are available or agreed, and none is evidence. A
  discreet **"Open in a new window"** escape hatch remains for a provider or browser that refuses
  framing.
- **Closing it reads the outcome.** ✕ or "I'm done" refreshes the profile, calls the latest
  endpoint, and shows what it says — with a "we couldn't check" state when the read fails.
- **Flow D** (`CashOutOutcomeStep.tsx` + `lib/payout/outcome.ts`): completed, cancelled, **expired
  worded distinctly from cancelled**, failed, neutral in-progress, still-setting-up, and unknown.
  Only completed gets the success treatment. No historical ZLTO figure anywhere — the USD amount and
  the updated wallet carry it instead.

### Also in this session — a CodeQL SSRF finding, unrelated to payout

CI flagged `api/services/marketplace.ts:44`: the `[country]` route segment was interpolated into an
API request path. `getStaticPaths` uses `fallback: "blocking"`, so **any** URL reaches
`getStaticProps`, and the request is then made server-side with the caller's credentials — a
segment containing `..` or an encoded slash could walk onto another endpoint. Encoding alone does
not fix that (`encodeURIComponent("..")` is still `".."`), so `lib/apiPath.ts` validates the shape
first: two letters for a country code, canonical hex for an id. The marketplace page now answers a
malformed country with `notFound` rather than an exception, and the same guard covers the
`[ruleId]` admin path, which has the identical exposure.

Checked in a browser: `/marketplace/WW` and `/marketplace/ZA` still 200, `/marketplace/ZAA` and
`/marketplace/a%2Fb` now 404, and `/marketplace/%2e%2e` never reaches the page at all — Next
collapses it to `/marketplace` before routing.

### First Dev pass — reached the hosted KYC, blocked there by a provider-side mapping

Ran the flow against Dev with the funded wallet. Steps 1–6 behaved: single debounced preview call,
the rate line matching `conversionRateZltoPerUsd` exactly (45 ZLTO = 1 USD, $2.22 for 100 ZLTO),
review agreeing with the amount step, `POST /user/payout/zlto?amount=100` with no body, and **the
provider's page rendering inside the iframe** — so framing is not the problem IXO thought it might
be. Adrian separately confirmed IXO supports embedding and that the Test `/pay` response carries
neither `X-Frame-Options` nor a framing CSP.

**The blocker is `personDetails.gender`.** The hosted verification step answers:

> Verification service error: 'personDetails.gender' should be equal to one of the allowed values:
> male, female, other (status code 400)

Traced end to end: Yoma's gender lookup is `Female` / `Male` / `Prefer not to say` (confirmed on
both local and `v3api.dev.yoma.world`), `PayoutService.InitiatePayout` sends `Gender = user.Gender`,
and `YellowCardClient.Validate` only trims it (`NormalizeRequired`) — nothing maps it. So all three
Yoma values are rejected: two for case, and "Prefer not to say" because it is not in the provider's
set at all. **This blocks the hosted journey for every user**, and it is API-side: the youth's
gender is a lookup id, so no UI change can affect what is sent. Needs a mapping —
`Male → male`, `Female → female`, anything else → `other`.

### Iframe sizing

The frame was not tracking the window. Fixed: the dialog is `md:h-[90vh] md:w-11/12
md:max-w-[1040px]` for the hosted step (was a fixed `720px`), its body is `overflow-hidden` with a
definite height instead of a scroll container, and the iframe carries `h-full min-h-0 w-full grow`.
`min-h-0` is the load-bearing part — a flex child's default `min-height: auto` will not shrink below
its content, and an iframe's content is another document, so without it the frame keeps its
intrinsic height and stops following the dialog. Verified at 1400×900, 1000×700 and 420×820.

**Unrelated observation while checking that:** at phone widths (390–420px) the *whole product*
overflows horizontally in headless Chrome — the home page's hero and Yoma's own consent banner clip
on the right just as much as anything in this flow. Reproduced on `/` , which this epic has never
touched, so it is neither the cash-out work nor the 2026-09-09 `PageBackground` change. Worth
confirming on a real device before anyone chases it.

## Current State

- Build: `tsc --noEmit` clean; `eslint` clean across `src`; `prettier --check` clean.
- Tests: n-a — `src/web` has no test runner.
- Migrations: n-a.
- Uncommitted work: none once this is committed.
- **Browser-verified** (headless Chrome via the Windows side, scratch pages deleted afterwards):
  all seven Flow D states; the resume panel with Started and in its not-yet-placed variant; and the
  hosted modal at its real size with a third-party page embedded, which confirms the modal, the
  stepper and the escape hatch lay out correctly around a real iframe.
- **Not exercised:** the live journey. Typing, `Max`, 1 → 2 → 3, a real POST, and the provider's
  page in the frame all need a logged-in session, which cannot be driven headless here
  (`yoma-web` has `directAccessGrantsEnabled: false`; only `admin-cli` permits a direct grant).

## Next Steps

1. **Session pass on Dev** with the funded wallet (2,000 ZLTO on `jason.dicker@didx.co.za`). Walk
   the flow, watch the debounced preview, initiate for real, and see whether the provider's page
   actually loads in the frame.
2. **Confirm framing with IXO** — if the hosted page refuses to be embedded, the escape hatch is
   what a youth would hit every time, and the iframe decision needs revisiting rather than shipping.
3. **The copy sheet.** Everything is still in one file, now including Flow D and the hosted-journey
   wording.
4. **Review the boards against what was built.** Flows B4 and D were drawn for a new-tab hand-off;
   the iframe replaces B4 entirely, and D gained two states the boards do not have (still-setting-up
   and unknown).

## Gotchas

- **`Processing` starts when the hosted payout is created, before the youth confirms anything.** So
  no screen names a status: the active trio is one neutral "in progress", and `canResume` decides
  only what is offered. Do not add a "waiting for you" / "processing" distinction off the back of
  `status` — it does not mean that.
- **A 404 from the session endpoint does not mean the payout closed.** It can originate at the
  provider and can be transient, so it routes to the outcome read rather than to a terminal message.
- **Never infer an outcome from the wallet.** A committed reservation and a released one both leave
  `pendingPayout` at zero.
- **`payout.amount` is USD; `zlto.pendingPayout` is ZLTO.** Still two figures for one payout, in two
  units, both called "amount".
- **Do not `POST` to refresh a session** — `GET /user/payout/zlto` issues a fresh one for the same
  payout. A second POST starts a second payout.
- The dev server needs `mise exec -- pnpm dev`: the default `node` on PATH is 20 and corepack's
  pnpm fails on it.
- The API's Swagger schema is served unauthenticated at `/swagger/v3/swagger.json` and is the
  fastest way to check a field's real shape. It is what caught the enum bug.

## Open Questions

- **For Adrian / IXO: does the hosted page allow being framed?** Everything else about the iframe
  is built and working; this is the one thing that decides whether it ships as the primary path.
- **For Adrian:** anything to be aware of in how `canResume` behaves right after initiation — is
  there a window where a payout is active with no provider reference in normal (non-failure)
  operation, or does that only follow an ambiguous attempt? The UI handles it either way; it changes
  how often a youth would see "we're still setting up".
- **For the copy owner:** Flow D's wording is a draft, in `lib/payout/copy.ts` with the rest. The
  choices most worth a second opinion are the expired-vs-cancelled split and the completed body
  ("The payment has been sent. Your wallet has been updated to match."), which deliberately avoids
  naming an amount of Zlto.
