# Handoff: forward-port released referral fix to master — 2026-09-23

## State

- Production release `v3.12.12` completed successfully. Adrian confirmed the affected referral usage displays Completed and 100% in the production UI.
- Branch `codex/referral-completed-progress-master` starts at `origin/master` commit `9501dcc96497187f62e2e9ec5db3ef1413e784b2` and cherry-picks the exact released `current` squash commit `9ddefa76f62fdd77493d1790b282d13682042770` as local commit `76e465837`.
- Cherry-pick had no conflicts. Production behavior is unchanged from the released fix.
- Master changed `IExecutionStrategyService.ExecuteInExecutionStrategyAsync` to accept a second optional bool. The referral test fixture's Moq callback must accept both arguments; that small test-only adjustment is pending commit.

## Verification

- API solution build: passed with zero warnings and errors.
- Three focused completed-versus-pending progress cases: passed after the master test fixture adjustment.
- The full `LinkUsageServiceTests` class has pre-existing unrelated claim-test failures. Its status lookup mock lacks Initiated and Abandoned; after a temporary diagnostic setup for those statuses, six tests still failed on stale validation-message assertions. Those exploratory changes were reverted to keep the forward-port scoped.

## Next

1. Commit the test fixture adjustment and this handoff on the master-based branch.
2. Push the branch and open a PR to `master`; verify checks and squash-merge.
3. Mark the feature shipped once master contains the fix.
