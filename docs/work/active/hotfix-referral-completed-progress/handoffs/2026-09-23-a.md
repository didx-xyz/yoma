# Handoff: completed referral progress hotfix — 2026-09-23

## State

- Branch: `hotfix/referral-completed-progress`, created from production-aligned `origin/current` at `9b18b25a4`.
- Local changes only. No commit, push, PR, release or production change yet.
- Stored `Completed` status now forces `PercentComplete = 100` and `EffectiveCompleted = true` in the computed usage detail response. The pathway detail is still populated as before. Pending usages retain the weighted progress calculation.
- No database migration or web change.

## Verification

- `dotnet build ../Yoma.Core.sln --no-restore --verbosity quiet`: succeeded with zero warnings and errors.
- Three focused `GetById_PathwayChangesPreserveCompletedProgress` cases: passed. They cover Completed with a pathway dated before and after completion, and Pending with incomplete current pathway progress.
- `dotnet format ../Yoma.Core.sln --no-restore --verify-no-changes --include domain/Yoma.Core.Domain/Referral/Services/LinkUsageService.cs test/Yoma.Core.Test/Referral/Services/LinkUsageServiceTests.cs`: passed.
- A broader `LinkUsageServiceTests` run had nine failures in existing `ClaimAsReferee` tests due to a null status lookup mock at `LinkUsageService.cs:336`; none entered the modified progress code. These failures were not fixed as part of the production hotfix.

## Next

1. Review and commit the API, regression test, and work-note changes.
2. PR to `current`, verify CI, release and confirm the affected referral usage shows Completed / 100% in production.
3. Bring the released fix into `master` and verify there as well.
